
var a = 10;
var b = 20;


// Conditional Operator (?:)

console.log((a > b) ? 100 : 200);       // ? : (Conditional)
console.log((a < b) ? 100 : 200);       // If Condition is true? Then value X : Otherwise value Y


// if a customer has ore than 100 points,
// they are a 'gold' customer, otherwise,
// they are a 'silver' customer.
let points = 110;
let type = points > 100 ? 'gold' : 'silver';
console.log(type);